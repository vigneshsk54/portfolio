# Public site URL — used for metadata, sitemap and OG tags
NEXT_PUBLIC_SITE_URL=https://vigneshkumars.dev

# Optional: connect the contact form to Formspree, Resend, or any endpoint of your choice.
# The form in components/Contact.tsx currently opens the user's mail client via a mailto: link
# and needs no key to function. Add one of the below only if you wire up a real backend.
# NEXT_PUBLIC_FORM_ENDPOINT=
# RESEND_API_KEY=
